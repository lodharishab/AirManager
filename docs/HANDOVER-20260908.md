# AirManager handover — 8 September 2026

## Deployed and synchronized

Application repair commit: `ccfb32d33a6def89a76e97b86892d37d86b3193e`.
Canonical repository: https://github.com/lodharishab/AirManager, branch `main`.
Production: `/opt/AirManager` on the existing VPS; HTTPS: https://zoella-vps.tailae8d17.ts.net:8443/.

The VPS previously used `feliciaofficial/AirManager` as origin, so pushes went to that fork. Origin now points to the canonical repository; main tracks origin/main. The old remote is retained as `legacy-fork`. AGENTS.md and docs/OPERATIONS.md document this requirement. Canonical MCP integration merge `7328e77` was incorporated without conflicts or force-pushing. The repair commit was pushed directly to canonical main; no repair PR is awaiting merge. VPS HEAD, origin/main and GitHub main matched after the push, and the working tree was clean.

## Repairs completed

- INR defaults, rupee displays/icons and invoice data lookup corrected.
- Checkout requires prior check-in. Invalid transitions, future check-in, invalid dates, foreign room assignments and insufficient nightly capacity are rejected. Update and concurrent-create paths are covered; database triggers enforce lifecycle and capacity and record status events. Undo actions support correction without deleting reservations.
- The reported accidental checkout is restored to Upcoming, with two rooms and September 12–13 dates. Live API and dashboard checks confirmed it.
- Rivaan total inventory is 11. Existing named room entries remain; nine rooms are explicitly unclassified, with unverified rates. Unsupported guest-capacity claims were removed; original property verification notes are preserved internally.
- Incorrect date uniqueness constraint replaced with capacity checks. Same room type can accept reservations up to its actual inventory.
- Preferences use the authenticated user and atomic creation; account routes require a session. Guest statistics follow guest IDs. Booking changes refresh dependent screens.
- Occupancy uses room-nights, and analytics uses recorded expenses instead of an invented 30%. Dashboard and analytics explain differing date scopes. Fake growth percentages were removed.
- AI chat uses configured provider/model with bounded read-only booking/property context, corrected suggestion input and visible error handling. Provider connectivity passed. External property enrichment uses validated fetching.
- Upload MIME/extension handling and image/video picker limits aligned. Mobile layout, calendar scrolling, booking status presentation and several control labels improved.
- Strict lint and typing issues repaired. Tests use disposable databases with a dedicated test role, not the production database.
- Structured logging adopted; API response bodies are not logged and server errors are sanitized. Production registration/demo seeding are disabled by default.
- Service runs as the dedicated airmanager OS account with systemd hardening, loopback binding and secure HTTPS proxy cookies.
- Exposed database/session/application API credentials were rotated. Secret values are excluded from Git and this document. MCP live checks confirm its upstream connection still works.
- Daily backups retain seven copies of database, source, configuration and media on the VPS.

## Verification

- TypeScript check passed.
- Strict ESLint passed with zero errors and zero warnings.
- 179 tests passed across seven files, including concurrency, lifecycle, preferences, aggregation and database-trigger coverage. Both migrations ran in disposable test databases.
- Production build succeeded. Remaining build warnings are listed below.
- 27 core read endpoints returned HTTP 200, including health, bookings, properties, guests, analytics, expenses, gallery, housekeeping, tickets and settings. Live database connectivity confirmed.
- AirManager, MCP and backup timer active; AirManager runs as airmanager, with zero automatic restarts and no new error-priority journal entries in the final check window.
- MCP smoke passed all applicable list/detail/dashboard/health tools. Expense detail could not use a live record because the expense ledger is empty.
- Final browser checks confirmed updated dashboard, Tickets, Housekeeping, Expenses, Reviews, Gallery and Settings/notification preferences render and load data. Earlier mobile review covered booking, property, guest, calendar, analytics and other operational pages. This is not a claim that every control and breakpoint was exhaustively exercised.
- Backup `/var/backups/airmanager/20260908T160222Z` was checksummed and restored successfully into an isolated temporary PostgreSQL database, which was then removed. Earlier rollback backup: `/var/backups/airmanager/20260908T152724Z`.

## Pending owner verification or setup

1. **Historical August 26–27 stays remain draft.** Verify actual stays, moves/refunds and room allocations separately. The original two-room overbooking assumption is not established against 11 total rooms. Historical booking and housekeeping notes remain preserved, including old unresolved wording.
2. **Rivaan room breakdown:** confirm the nine unclassified rooms' types, rates and total guest capacity. Existing named room types/counts also need ground-truth confirmation before assigning OTA room types confidently.
3. **Business data:** review incomplete property/address notes, original review text/guest identity, expense entries and payout reconciliation. Booking value is not bank income; recorded-expense profit is incomplete until expenses are entered. No historical amounts or attendance were invented.
4. **Off-server backups:** choose an encrypted destination. Current automatic backups do not protect against complete VPS loss.
5. **Google Drive import:** requires its missing API key. The application now reports unavailable setup clearly. No credential was invented.
6. **Notification delivery:** preferences load correctly, but end-to-end external email/push delivery and OTA/channel ingestion were not certified in this audit. Verify provider configuration and real delivery before depending on them.
7. **Roles:** the application remains a shared-authority authenticated system. Per-user/per-property roles are a separate feature requiring access rules. MCP client tokens currently expose all permitted read tools, as documented in its README.

## Remaining engineering work

- Production build emits a large client bundle warning and five CommonJS/import.meta warnings from bundled development Vite configuration. Build and deployed runtime pass, but code splitting and development-only bundling cleanup remain.
- server/routes.ts and server/storage.ts remain large. Domain extraction was not bundled into the integrity repair; it needs a focused refactor with behavior-preserving tests.
- Deployment remains a validated manual build/restart workflow. Automated CI deployment has not been configured; always push to canonical main and verify deployed source/build together.
- Complete desktop, invoice/print, 404 and exhaustive interaction/accessibility QA. Some older icon buttons in housekeeping, reviews and gallery still lack accessible labels; Gallery's summary still calls mixed media “images.” Media transcoding/full content-signature verification are not implemented.
- Scheduler long-duration behavior after restart still needs observation across scheduled intervals. Empty recommendations or tickets alone do not prove failure.
- Old revoked secrets remain in historical Git objects. History was not rewritten or force-pushed. Maintain normal credential rotation, and update MCP's private upstream environment if rotating the application API key again.

## Next deployment / recovery

Follow docs/OPERATIONS.md. Validate with `npm run check`, `npm run lint`, `npm test` using `.env.test`, and `npm run build`. Back up before changes; apply reviewed migrations, install source/build and restart the service. Never run tests or forced schema pushes against production. Do not commit `.env`, uploads, exports, dependencies or dist.

For code rollback, inspect the saved application archive in a staging directory and restore the appropriate code/build. Do not blindly restore pre-rotation environment credentials or overwrite newer booking data. Database rollback is a separate maintenance decision.
